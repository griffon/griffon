apply plugin: 'groovy'

ext.pomDescription = 'Griffon Swing Builder module'

configurations {
    compileOnly
}

dependencies {
    compile project(':griffon-groovy')

    compileOnly project(':griffon-core-compile')
}
