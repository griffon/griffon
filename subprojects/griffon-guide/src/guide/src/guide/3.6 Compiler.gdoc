The following sections explain settings that affect how the compiler behaves

Most of these settings may specified in either @griffon-app/conf/BuildConfig.groovy@ (local to project) or @$USER_HOME/.griffon/settings.groovy@ (global to all projects), with the caveat that values specified at the command prompt will have precedence over those specified in the config file.

