Griffon applications can be run in standalone mode using the [run-app|commandLine] command:

{code}
griffon run-app
{code}

Or in applet mode using the [run-applet|commandLine] command:

{code}
griffon run-applet
{code}

Or in webstart mode using the [run-webstart|commandLine] command:

{code}
griffon run-webstart
{code}

More information on the [run-app|commandLine] command can be found in the reference guide.