h1. compile 

h2. Purpose

The @compile@ command will execute the compile phase of the Griffon pre-packaging process, which pre-compiles Groovy and Java sources.

h2. Examples

{code}
griffon compile
{code}

h2. Description

Usage: 
{code:java}
griffon compile
{code}

Options:
* @clean@ - Clean project before compiling.
