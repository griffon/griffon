DESCRIPTION
        list-archetype-updates

	Lists all available archetype updates.

SYNTAX
        list-archetype-updates [options]

OPTIONS
        --install
                Install all matching upgrades.
        --non-interactive
                Controls if the shell can ask for input or not.
        --repository
                Name of an specific repository where the search will be performed.
