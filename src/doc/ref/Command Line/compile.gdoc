h1. compile 

h2. Purpose

The @compile@ command will execute the compile phase of the Griffon pre-packaging process, which pre-compiles Groovy and Java sources.

h2. Examples

bc.
griffon compile

h2. Description

Usage: 
{code:java}
griffon compile
{code}

Fired Events: 

* @CompileStart@ - Before compilation begins
* @CompileEnd@ - After compilation completes
