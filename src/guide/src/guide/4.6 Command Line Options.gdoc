The following command line options only have meaning while building the project. They have no effect when running the application once it has been [packaged|guide:packaging].

It's worth noting that all of the following options can also be specified in @griffon-app/conf/BuildConfig.groovy@, with the caveat that values specified at the command prompt will have precedence over those specified in the config file.
