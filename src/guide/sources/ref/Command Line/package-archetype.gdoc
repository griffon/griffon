h1. package-archetype 

h2. Purpose

Packages an archetype so that it can be later installed. Simply generates a zip file with all contents of the archetype's directory. The name of the archetype directory will be used as the name of the archetype.

h2. Examples

{code:java}
griffon package-archetype
{code}

h2. Description

Usage: 
{code:java}
griffon package-archetype
{code}
