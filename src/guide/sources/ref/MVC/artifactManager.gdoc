h1. artifactManager

h2. Purpose

Property that points to the ArtifactManager instance.

h2. Description

The @ArtifactManager@ is responsible for handling all artifact related classes. You can query it for info on any artifact that is resgistered within the application.
