h1. app

h2. Purpose

Property that points to the application instance.

h2. Description

Every MVC group member and every artifact that was created using the @NewInstance@ dynamic method will have a reference to this property via its metaclass. The *app* variable is the key for accessing all the application [features|guide:applicationFeatures] like configuration, [event publishing|guide:applicationEvents] and [threading|guide:threading].
