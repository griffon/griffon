This phase is responsible for instantiating all MVC groups that have been defined on the application's configuration (@Application.groovy@) and that also have been marked as startup groups in the same configuration file.

{note}
The @Startup@ script will be called *after* all MVC groups have been initialized.
{note}
