The application's runtime configuration is available through the *config* property of the application instance. This is a @ConfigObject@ whose contents come from the @Application.groovy@ file.

Builder configuration is available through the @builderConfig@ property.
