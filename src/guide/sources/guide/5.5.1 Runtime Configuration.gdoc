The application's runtime configuration is available through the *config* property of the application instance. This is a @ConfigObject@ whose contents come from merging @Application.groovy@ and @Config.groovy@.

Builder configuration is available through the @builderConfig@ property.
