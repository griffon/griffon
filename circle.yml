machine:
  java:
    version: oraclejdk8

dependencies:
  override:
    - ./gradlew -q dependencies

test:
  override:
    - ./gradlew -q check test integrationTest

general:
  branches:
    only:
      - master
      - development