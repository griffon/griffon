machine:
  java:
    version: oraclejdk8

dependencies:
  override:
    - ./gradlew -q dependencies
 cache_directories:
    - ".gradle"

test:
  override:
    - ./gradlew -q test integrationTest

general:
  branches:
    only:
      - master
      - development
    ignore:
      - gh-pages