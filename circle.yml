machine:
  java:
    version: oraclejdk8

dependencies:
  override:
    - ./gradlew dependencies

test:
  override:
    - ./gradlew check test integrationTest

general:
  branches:
    only:
      - master
      - development